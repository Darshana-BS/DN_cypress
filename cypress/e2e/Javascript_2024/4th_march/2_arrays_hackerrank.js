const prompt = require('prompt-sync')();

class staffLine{
    constructor(staff)
    {
        this.staff = []
    }
}
for(i= 0; i<10; i++)
{
    let details = prompt ('enter the list of array elements')
    // array.push(i)
    // this.staff.push(details)

    // console.log(array)
}
console.log(details)